name: Frontend CI/CD

on:
  push:
    branches: [ main, dev ]
    paths:
      - 'PluckIt.Client/**'
      - '.github/workflows/frontend-ci.yml'
  pull_request:
    branches: [ main, dev ]
    paths:
      - 'PluckIt.Client/**'
      - '.github/workflows/frontend-ci.yml'

concurrency:
  group: azure-production-frontend
  cancel-in-progress: false

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: PluckIt.Client

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: PluckIt.Client/package-lock.json
    
    - name: Install dependencies
      run: npm install
    
    - name: Unit Tests
      run: npm test -- --no-progress --watch=false

    - name: Build Production
      run: npm run build
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: pluckit-client
        path: PluckIt.Client/dist/PluckIt.Client/browser/

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Download artifact
      uses: actions/download-artifact@v4
      with:
        name: pluckit-client
        path: ./dist/browser
    
    - name: Deploy to Azure Static Web Apps
      uses: Azure/static-web-apps-deploy@v1
      with:
        azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        action: 'upload'
        app_location: './dist/browser'
        skip_app_build: true

