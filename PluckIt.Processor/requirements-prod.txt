azure-functions
azure-storage-blob>=12.20.0
azure-storage-queue>=12.0.0
azure-cosmos>=4.7.0
pillow>=10.3.0
pillow-heif>=0.16.0
urllib3>=2.2.2

# Web framework (ASGI) for SSE streaming
fastapi
python-multipart  # required by FastAPI/Starlette for multipart/form-data parsing

# OpenTelemetry SDK + OTLP exporter for Grafana Cloud observability
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-httpx

# LangGraph / LangChain for the stylist agent
langgraph
langchain-openai
langchain-core

# Langfuse tracing for LLM calls and tool executions
langfuse
aiohttp
# HTTP client for Open-Meteo weather API (no API key needed)
httpx

# Google ID token validation (used to authenticate Angular clients directly)
google-auth

# Fashion RSS feed scraping for mood processor
feedparser

# Fashion content scraper
imagehash>=4.3.1        # perceptual hashing for image deduplication
playwright>=1.40.0      # headless browser for JS-heavy brand sites (Phase 3)
